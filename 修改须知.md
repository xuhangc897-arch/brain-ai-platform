# 修改须知

> 这是本项目后续修改的强制入口。开始任何代码、配置、数据或部署修改前，必须完整阅读本文件；完成修改后必须同步更新本文件涉及的规则、`docs/HARDENING_CHANGELOG.md` 和 `docs/HARDENING_GUARDRAILS.md`。

## 一、开始修改前必须做

1. 运行 `git status -sb`，确认当前分支和用户已有修改。
2. 阅读：
   - 本文件。
   - `docs/HARDENING_GUARDRAILS.md`。
   - `docs/HARDENING_CHANGELOG.md` 最新一项。
   - 与任务对应的专项文档。
3. 写明影响范围、明确不修改的功能和验证标准。
4. 如果可能影响学生实验步骤、计时、评分、随机化、解锁、作答或历史数据，先暂停并说明风险。
5. 只修改本任务涉及的文件，不顺手清理相邻代码。

## 二、当前架构入口

- 实验注册表：`assets/experiment-registry.js`
- 平台配置、身份与记录契约：`assets/platform-core.js`
- 可靠上传与 outbox：`assets/experiment-uploader.js`
- 完整状态提交与报告收尾：`assets/experiment-bridge.js`
- 五个实验页面状态生命周期：`assets/experiment-page-runtime.js`
- 教师后台、报告与 AI 数据适配：`assets/experiment-integration.js`
- 报告构建：`assets/review.js`
- AI 助手：`assets/ai-assistant.js`
- 教师后台：`admin/dashboard.html`

不得在单个页面重新硬编码模块 ID、模块名称、报告路由、CloudBase 地址、公共 storage key 或 AI 地址。

## 三、学生实验不可随普通重构改变

- 步骤顺序和步骤数量。
- 前进、返回和最大解锁步骤。
- 完成条件和提示文案对应关系。
- 计时开始/停止、随机材料、刺激呈现和作答顺序。
- 评分公式、正确答案、统计口径和报告结论。
- localStorage key、历史字段迁移和 `clientRecordId`。
- 保存、AI 对话上传、报告打开的先后顺序。

确需改变时，必须作为独立业务任务，提供旧数据兼容、页面级测试、人工验收和回滚方案。

## 四、数据与安全禁止事项

- 不把浏览器登录态当作服务端授权。
- 不信任客户端传入的学生身份或教师角色。
- 不记录或返回密码、token、密钥、签名、完整学生记录、AI 对话正文或原始异常对象。
- 不批量覆盖、删除或回填历史数据库记录，除非有备份、dry-run、数量校验和回滚。
- 不修改 storage key、schemaVersion 或 `clientRecordId` 算法而不提供迁移。
- 数据库备份不得进入静态发布包。

## 五、每次修改的最低验证

```bash
npm run verify
npm run build
npm run check:dist
```

还必须根据范围增加：

- 实验页：刷新恢复、前后步骤、验证、实验操作、报告生成。
- AI：空记录、正常问答、失败提示、来源模块和上传。
- 报告：五个合法活动和非法 activityType 回退。
- 教师后台：查询、筛选、详情、七模块导出。
- 上传：在线、离线、恢复联网、重复提交、共享设备切换学生。
- 云函数：成功、无权限、无效输入、旧版本和暂时失败。

测试失败时先修复首个真实失败，不得通过放宽断言掩盖行为变化。

## 六、构建与部署

- Node.js 构建版本：20.19。
- `npm run build` 生成最小 `dist/`，不得直接发布整个仓库。
- GitHub Pages 使用 `.github/workflows/pages.yml`。
- Pages Source 必须设置为 `GitHub Actions`。
- Pull Request 只构建不部署；合并 `main` 后部署。
- 部署后立即运行只读冒烟；定时监控每小时运行。
- 公共脚本与调用页面必须同一版本发布，禁止手工只覆盖单个文件。
- 涉及数据库 schema 时先部署兼容云函数，再部署静态页面；回滚顺序相反。

完整说明：`docs/BUILD_DEPLOY_MONITOR.md`。

## 七、文档维护

每次修改后必须：

1. 在 `docs/HARDENING_CHANGELOG.md` 追加边界清晰的任务记录。
2. 在 `docs/HARDENING_GUARDRAILS.md` 补充可复用护栏。
3. 若架构、契约、构建或部署变化，更新对应专项文档。
4. 报告修改文件、修改原因、自动测试、人工测试和未验证项目。

## 八、当前仍未解决的遗留

- 教师后台临时免登录。
- 服务端授权和学生数据归属校验。
- 明文或可推导密码及密码迁移。
- CloudBase Node.js 运行时仍为 16.13。
- 数据库备份仍在 Git 仓库中，虽然不会进入 Pages 发布包。
- AI 后端密钥、限流、CORS 和真实环境日志治理。

详细登记：`docs/LEGACY_REGISTER.md`。不得因为阶段 1～7 已完成而把这些项目标记为已解决。

## 九、后续向 Codex 提交任务时

建议在请求开头写：

```text
请先完整阅读根目录《修改须知.md》，再处理本次任务。
只修改本任务涉及的文件，不改变学生实验流程。
修改后更新变更记录和护栏，并告诉我如何测试。
```
