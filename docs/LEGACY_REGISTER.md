# 遗留问题登记表

阶段 7 只清理已证明无引用且可通过 Git 恢复的低风险文件。以下项目仍然存在，不能被视为已解决。

## 高风险保留项

### 教师后台临时免登录

- 位置：`admin/dashboard.html`
- 现状：`TEMPORARY_DASHBOARD_LOGIN_BYPASS = true`
- 风险：前端登录遮罩被绕过。
- 未直接清理原因：关闭后可能中断当前教师使用，且服务端授权尚未完成。
- 后续条件：先建立正式教师认证、服务端角色校验和回滚账号，再删除。

### 服务端授权与数据归属

- 位置：实验记录保存/查询及学生相关云函数。
- 风险：浏览器身份不能作为可信授权，客户端字段仍可能伪造。
- 后续条件：设计服务端会话、角色和学生数据归属校验。

### 明文或可推导密码

- 位置：学生导入、登录与默认密码流程。
- 风险：凭证强度和泄露影响。
- 后续条件：设计密码哈希、重置流程和旧账号迁移。

### CloudBase Node.js 运行时

- 位置：`cloudbase.json`
- 现状：云函数声明 Node.js 16.13，前端构建使用 Node.js 20.19。
- 风险：运行时支持周期和本地/云端差异。
- 未直接修改原因：需要先确认 CloudBase 当前支持版本并在测试环境部署四个云函数。

### 数据库备份

- 位置：`备份/`
- 现状：包含阶段 3 前后的数据库导出。
- 风险：可能包含学生数据，不应进入 Pages 发布包或公开下载。
- 处理：继续保留用于回滚，但构建检查保证它不进入 `dist/`。
- 后续条件：迁移到受控加密存储，并从公开代码仓库历史中治理。

### AI 后端与密钥管理

- 位置：`api/`、AI 云函数及部署平台环境变量。
- 风险：密钥、限流、调用审计和上游失败治理仍依赖外部平台配置。
- 后续条件：在真实环境核对密钥权限、配额、CORS 和日志脱敏。

## 阶段 7 已清理

- 删除根目录无页面引用的旧 `ai-assistant.js`。
- 删除无外部引用的 `脑育智能体/` 历史页面副本。
- 替换占位 `README.md`。
- GitHub Pages 发布包不再包含整个仓库。
